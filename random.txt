public List<PermissionDTO> getAllPermissions() {
        return permissionRepository.findAll().stream()
                .map(permission -> new PermissionDTO(permission.getPermissionRid(), permission.getPermissionName()))
                .collect(Collectors.toList());
    }