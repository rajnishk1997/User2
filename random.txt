 // Extract the CN value (prai8 in this case)
            String managerCN = null;
            if (manager != null && manager.startsWith("CN=")) {
                String[] parts = manager.split(",");
                for (String part : parts) {
                    if (part.startsWith("CN=")) {
                        managerCN = part.substring(3); // Extract the value after "CN="
                        break;
                    }
                }
            }