 @Query("SELECT r FROM RXSOTProjectDetail r WHERE r.currentUserId = :currentUserId AND r.validationStatus = :validationStatus")
    List<RXSOTProjectDetail> findPendingSotsByCurrentUserIdAndValidationStatus(
        @Param("currentUserId") Long currentUserId,
        @Param("validationStatus") String validationStatus
    );
	
	 Long currentUserId = getCurrentUserId();  // Replace this with your logic to get current user ID

        // Call the DAO method to get pending SOTs with ValidationStatus = 'Y'
        return tempDao.findByCurrentUserIdAndValidationStatus(currentUserId, "Y");