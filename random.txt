 // Step (iii.a): If value ends with "RT" followed by a number, replace with "SP" followed by the same number
    if (valueWithoutSpaces.matches(".*RT\\d+$")) {
        replacedValue = valueWithoutSpaces.replaceAll("RT(\\d+)$", "SP$1");
        GppJson28Field pair = findInList(filteredList, replacedValue);
        if (pair != null) return pair;
    }

    // Step (iii.b): If no cases above are fulfilled, append "SP" at the end
    replacedValue = valueWithoutSpaces + "SP";
    GppJson28Field pair = findInList(filteredList, replacedValue);
    if (pair != null) return pair;