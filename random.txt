  @Query("SELECT CONCAT(m.userFirstName, ' ', COALESCE(m.userMiddleName, ''), ' ', m.userLastName) " +
           "FROM User u JOIN u.manager m WHERE u.userRid = :userRid")
    String findManagerFullNameByUserRid(@Param("userRid") int userRid);
	
	
	 @Query("SELECT u.manager.userRid FROM User u WHERE u.userRid = :userId")
    Integer findManagerIdByUserId(@Param("userId") int userId);