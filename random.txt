  @Query("SELECT new com.example.dto.RXSOTProjectDetailDTO(p.id, CONCAT(m.userFirstName, ' ', COALESCE(m.userMiddleName, ''), ' ', COALESCE(m.userLastName, ''))) " +
           "FROM RXSOTProjectDetails p " +
           "JOIN User m ON p.managerRid = m.userRid " +
           "WHERE p.id = :projectId")
    RXSOTProjectDetailDTO findProjectDetailWithManagerName(@Param("projectId") int projectId);
}