@Query("SELECT u FROM User u LEFT JOIN u.userRoles ur " +
       "WHERE (u.manager.userRid = :managerId AND ur.role.roleName = 'Auditor') " +
       "OR (u.userRid = :managerId AND ur.role.roleName IN ('Auditor', 'Manager'))")
List<User> findAuditorsAndManagersUnderHierarchy(@Param("managerId") Integer managerId);
