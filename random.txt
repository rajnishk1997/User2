  public List<SOTNetworkMaster> getNetworksByPlatform(String platformName) {
        // Fetch all SOTNetworkMaster entries based on platform name in a single query
        return sotNetworkMasterRepository.findAllByPlatformName(platformName);
    }
	
	// Single query to fetch all SOTNetworkMaster entries based on platformName in SPlatform
    @Query("SELECT sn FROM SOTNetworkMaster sn WHERE sn.platform.platformName = :platformName")
    List<SOTNetworkMaster> findAllByPlatformName(@Param("platformName") String platformName);