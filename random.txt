// Assuming gppFieldsList is already populated
Map<String, Long> validationStatusCounts = gppFieldsList.stream()
    .flatMap(gppFields -> gppFields.values().stream())
    .collect(Collectors.groupingBy(GppFieldValidationResponse.FieldValidation::getValidationStatus, Collectors.counting()));

// Print final counts
System.out.println("Final counts: null=" + validationStatusCounts.getOrDefault("null", 0L) +
                   ", true=" + validationStatusCounts.getOrDefault("true", 0L) +
                   ", false=" + validationStatusCounts.getOrDefault("false", 0L));






// Count "validationStatus" = "true"
long trueCount = gppFieldsList.stream()
    .flatMap(gppFields -> gppFields.values().stream())
    .filter(validation -> "true".equals(validation.getValidationStatus()))
    .count();

// Count "validationStatus" = "false"
long falseCount = gppFieldsList.stream()
    .flatMap(gppFields -> gppFields.values().stream())
    .filter(validation -> "false".equals(validation.getValidationStatus()))
    .count();

// Count "validationStatus" = "null"
long nullCount = gppFieldsList.stream()
    .flatMap(gppFields -> gppFields.values().stream())
    .filter(validation -> "null".equals(validation.getValidationStatus()))
    .count();

// Print final counts
System.out.println("Final counts: null=" + nullCount + ", true=" + trueCount + ", false=" + falseCount);