  @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        SOTNetworkMaster that = (SOTNetworkMaster) o;
        return sRid == that.sRid;
    }

    @Override
    public int hashCode() {
        return Objects.hash(sRid);
    }